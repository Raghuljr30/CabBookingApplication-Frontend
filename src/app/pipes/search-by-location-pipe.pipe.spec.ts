import { SearchByLocationPipePipe } from './search-by-location-pipe.pipe';

describe('SearchByLocationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByLocationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
